import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AK5PM_project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
